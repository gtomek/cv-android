<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:background="@android:color/white"
        android:clickable="true"
        android:focusable="true"
        android:padding="@dimen/gap_small"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    <TextView
            android:id="@+id/textview_summary"
            android:layout_width="0dp"
            android:layout_margin="@dimen/gap_small"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:textStyle="bold"
            tools:text="Very basic summary of the candidate"/>

    <TextView
            android:id="@+id/textview_knowledge"
            android:layout_width="0dp"
            android:layout_margin="@dimen/gap_small"
            android:layout_height="wrap_content"
            android:paddingTop="@dimen/gap_small"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/textview_summary"
            tools:text="knowledge: Java, Ruby, JavaScript"/>

</androidx.constraintlayout.widget.ConstraintLayout>

